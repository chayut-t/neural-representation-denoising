# Repository-owned synthetic RGB fixture (decision 0001): CI/tests only, never
# scientific evidence. Deterministic from (seed, index, size); safe to regenerate.
name: synthetic
official_url: null  # generated by neural_repr.data.synthetic
terms_acceptance_token: null  # licensed with the code
manifest: data/manifests/synthetic.csv
n_images: 8
size: 32
seed: 0
file_extension: .png
